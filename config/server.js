export const ServerConfig = {
    PORT: 3000
};

export const ResponseCode = {
    ERROR_NAME_ALREADY_EXIST : -1,
    ERROR_SOCKET_ALREADY_EXIST: -2,
    ACCOUNT_CREATED : 1
};

export default ServerConfig;